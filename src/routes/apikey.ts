import { Router } from 'express';
import axios from 'axios';
import { prisma } from '..';
import { generateAPIKey, generatePrefix } from '../lib/apikey';

const router = Router()





export default router;
